<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Custom permission that third-party apps must declare to bind to AiModelHubService.
         protectionLevel="normal" means any app can request it without user approval. -->
    <permission
        android:name="com.ai_model_hub.permission.BIND_SERVICE"
        android:protectionLevel="normal" />

    <application
        android:name=".AiModelHubApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiManager">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AiManager"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager foreground service: must declare type on Android 14+ -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- AIDL Bound Service for inter-process AI inference -->
        <service
            android:name=".service.AiModelHubService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:permission="com.ai_model_hub.permission.BIND_SERVICE">
            <intent-filter>
                <action android:name="com.ai_model_hub.service.AiModelHubService" />
            </intent-filter>
        </service>

    </application>

</manifest>
